namespace SimplePaint
{
	/// <summary>
	/// Defines states that the drawing form can be set to.
	/// </summary>
	public enum FormState
	{
		DrawFigure,
		AddVertex,
		ChangeColor,
		Multisampling,
		ChangeThickness,
		Default
	}
}
